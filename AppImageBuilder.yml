version: 1

script:
  - |
    set -eu

    if [ "{{APPIMAGE_SOURCE}}" != "${TARGET_APPDIR}" ]; then
      mv "{{APPIMAGE_SOURCE}}" "${TARGET_APPDIR}"
    fi

    # Manual installation of squashfs-tools is required
    # until https://github.com/AppImageCrafters/build-appimage/issues/5 is fixed
    if ! command -v mksquashfs >/dev/null; then
      apt-get update && apt-get install -y --no-install-recommends squashfs-tools
    fi

AppDir:
  app_info:
    id: io.github.c0rn3j.sc-controller
    name: sc-controller
    version: "{{APPIMAGE_VERSION}}"
    icon: sc-controller
    exec: usr/bin/python3
    # Everything after the import and before os.execvp() are hacks to make Glycin work
    exec_args: -c 'import os, sys; appdir = os.environ["APPDIR"]; library_path = os.environ.get("LD_LIBRARY_PATH", ""); os.environ["LD_LIBRARY_PATH"] = (f"{appdir}/usr/lib" + (f":{library_path}" if library_path else "")); preload = os.environ.get("LD_PRELOAD", "");os.environ["LD_PRELOAD"] = ("appimage-glycin-sandbox-hack.so:appimage-glycin-anylinux-hack.so" + (f":{preload}" if preload else ""));os.execvp("$APPDIR/usr/bin/scc", ["$APPDIR/usr/bin/scc"] + (sys.argv[1:] if len(sys.argv) > 1 else ["gui"]))' $@

  after_bundle: |
    set -eu

    # Expose bundled Glycin loaders in the AppImage's bin directory.
    for loader in "${TARGET_APPDIR}"/usr/libexec/glycin-loaders/*/glycin-*; do
      [ -f "${loader}" ] && [ -x "${loader}" ] || continue
      mkdir -p "${TARGET_APPDIR}/bin"
      ln -sfr "${loader}" "${TARGET_APPDIR}/bin/"
    done

    # Remove host-specific absolute paths from bundled Glycin loader commands.
    glycin_conf_dir="${TARGET_APPDIR}/usr/share/glycin-loaders"
    if [ -d "${glycin_conf_dir}" ]; then
      find "${glycin_conf_dir}" -type f -name '*.conf' -exec sed -i -e 's|/usr/lib.*/||g' {} +
    fi

    # install python-hidraw-pure and python-ioctl-opt manually as they were only packaged in Debian 14
    # TODO(Martin): Replace URL with just 'hidraw-pure' when https://github.com/vpelletier/python-hidraw/issues/7 is resolved
    pip install --target "${TARGET_APPDIR}/usr/lib/python3/dist-packages/" https://github.com/vpelletier/python-hidraw/archive/master.zip ioctl-opt

    # The wheel is installed under a versioned site-packages directory. Give
    # the runtime a stable path to it without adding the executable $PATH to
    # $PYTHONPATH (which makes gi/types.py shadow the stdlib types module).
    scc_site_packages=$(find "${TARGET_APPDIR}/usr/lib" -type d -path '*/python*/site-packages' -exec test -d '{}/scc' ';' -print -quit)
    test -n "${scc_site_packages}"
    ln -sfr "${scc_site_packages}" "${TARGET_APPDIR}/usr/lib/scc-site-packages"

    # appimage-builder expects .desktop file to start with appinfo-id
    desktop="$(find "${TARGET_APPDIR}/usr" -name sc-controller.desktop)"
    sed -i "s:Exec=.*:Exec=./usr/bin/scc gui:g" "${desktop}"
    ln -sr "${desktop}" "${TARGET_APPDIR}/usr/share/applications/io.github.c0rn3j.sc-controller.desktop"

    # appimage-builder expects utf-8 encoding when patching shebangs,
    # but pygettext3 has iso-8859-1 encoding
    find "${TARGET_APPDIR}/usr/bin" -name 'pygettext*' | while read -r file; do
      encoding="ISO-8859-1"
      if file -bi "${file}" | grep -iq "${encoding}"; then
        <"${file}" iconv -f "${encoding}" -t utf-8 -o "${file}"
        sed -i -E '1,2 s|^(\s*#.*coding[=:]\s*)([[:alnum:].-]+)|\1utf-8|g' "${file}"
      fi
    done

    # remove unused icons
    icons="${TARGET_APPDIR}/usr/share/icons"
    keep=$(
      # Subset of: `wget -q https://specifications.freedesktop.org/icon-naming-spec/latest -O- | sed -nE 's:.*</td></tr><tr><td>([^<]+).*:\1:p' | sort`
      printf '%s\n' index.theme close- edit- go- list- pan- window-
      grep -rhoP '"icon_name">\K[^<]+(?=<)' glade
      grep -rhoP 'icon_name *= *"\K[^"]+(?=")' scc
    )
    map_item() { echo "${1}" | while read -r item; do printf -- "${2}" "${item}"; done; }
    paths=$(find "${icons}" '(' -false $(map_item "${keep}" ' -o -name %s*') ')' -print -type l -printf '%h/%l\n' | sort | uniq)
    find "${icons}" \( -type d -empty -or -not -type d $(map_item "${paths}" ' -not -samefile %s') \) -delete
    cancel() { echo >&2 "ERROR: ${@}" && return 1; }
    if find "${icons}" -xtype l | grep .; then
      cancel 'The symlinks shown above are broken.'
    fi
    if echo "${keep}" | while read -r icon; do echo "${paths}" | grep -q "${icon}[^/]*$" || echo "${icon}"; done | grep .; then
      cancel 'The icons above are missing.'
    fi

    # Enable icon theme
    xdg_settings="${TARGET_APPDIR}/etc/xdg/gtk-4.0/settings.ini"
    if [ ! -e "${xdg_settings}" ]; then
      mkdir -p "$(dirname "${xdg_settings}")"
      theme=$(find "${icons}" -mindepth 1 -maxdepth 1 -type d -printf %f -quit)
      printf '[Settings]\ngtk-icon-theme-name = %s\n' "${theme}" >"${xdg_settings}"
    fi

    # python3-usb1 expects 'libusb-1.0.so', see https://github.com/vpelletier/python-libusb1/issues/78
    find "${TARGET_APPDIR}" -name 'libusb-1.0.so.[0-9]' | while read -r file; do
      path="$(dirname "${file}")"
      link="${file%.[0-9]}"
      [ -e "${link}" ] || ln -sr "${file}" "${link}"
    done

  after_runtime: |
    set -eu

    # python3 is linked against 'lib64/ld-linux-x86-64.so.2' but 'compat/lib64' is missing
    compat="${TARGET_APPDIR}/runtime/compat"
    if [ ! -e "${compat}/lib64" ] && [ -d "${compat}/usr/lib64" ]; then
      ln -s "usr/lib64" "${compat}/"
    fi

  apt:
    arch:
      - "{{APPIMAGE_APT_ARCH}}"

    sources:
      - sourceline: deb [arch=amd64] http://archive.ubuntu.com/ubuntu/ {{APPIMAGE_APT_DISTRO}} main universe
        key_url: http://keyserver.ubuntu.com/pks/lookup?op=get&search=0x{{APPIMAGE_APT_PUBKEY}}
      - sourceline: deb [arch=amd64] http://archive.ubuntu.com/ubuntu/ {{APPIMAGE_APT_DISTRO}}-updates main universe
      - sourceline: deb [arch=amd64] http://security.ubuntu.com/ubuntu/ {{APPIMAGE_APT_DISTRO}}-security main universe
      - sourceline: deb [arch=arm64] http://ports.ubuntu.com/ubuntu-ports/ {{APPIMAGE_APT_DISTRO}} main universe
      - sourceline: deb [arch=arm64] http://ports.ubuntu.com/ubuntu-ports/ {{APPIMAGE_APT_DISTRO}}-updates main universe
      - sourceline: deb [arch=arm64] http://ports.ubuntu.com/ubuntu-ports/ {{APPIMAGE_APT_DISTRO}}-security main universe

    include:
      - gir1.2-gtk-4.0
      - gir1.2-rsvg-2.0
      - libbluetooth3
      - libgirepository-2.0-0
      - libgtk-4-1
      - libgtk4-layer-shell-dev
      - gir1.2-gtk4layershell-1.0
      - librsvg2-common
      - libwayland-client0
      - libwayland-cursor0
      - libwayland-egl1
      - python-is-python3
      - python3-evdev
      - python3-gi-cairo
      - python3-pylibacl
      - python3-usb1
      - python3-vdf
      - binutils         # required for detection of bluetooth library
      - coreutils        # provides /usr/bin/env
      - paper-icon-theme # required for icons
      - shared-mime-info # required for gui if host provides no MIME info, e.g. when XDG_DATA_DIRS is missing

    exclude:
      # coreutils
      - libgmp*         # arithmetics

      # gir1.2-rsvg-2.0
      # NOTE: libicu* and libxml* are intentionally NOT excluded. The bundled
      # librsvg needs libxml2 (which pulls in icu), and minimal hosts such as
      # SteamOS do not provide them, so excluding them gives a fatal
      # "libxml2.so.2: cannot open shared object file" at GUI startup.
      - libstdc*        # development

      # libgtk3-0
      - "*-icon-theme"  # gui
      - "*crypt*"       # security
      - "*dconf*"       # gui configuration
      - "libdbus-1*"    # process communication
      - "*gcc*"         # development
      - "*systemd*"     # linux core
      - libblk*         # filesystem
      - libcolord*      # gui
      - libcups*        # printing
      - libepoxy*       # gui
      - libfontconfig*  # fonts
      - libfreetype*    # fonts
      - libfribidi*     # i18n
      - libgtk-4-common # gui
      # - liblz*          # codec
      # - libtiff*        # codec
      # - libwebp*        # codec

      # python3
      - "*krb*"         # security
      - "*readline*"    # terminal
      - "*sqlite*"      # database
      - libbz*          # codec
      - libdb*          # database
      - libmpdec*       # arithmetics
      - libncurses*     # terminal
      - libnsl*         # network
      - libssl*         # security
      - libtirpc*       # development
      - libuuid*        # development
      - media-types     # codec

      # Ubuntu Noble
      - libgprofng*     # development
      - libjansson*     # codec
      - libsframe*      # development
      - netbase         # network
      - tzdata          # date/time

  files:
    exclude:
      - usr/bin/*gold*              # alternative for ld
      - usr/bin/*gp-display-html*   # requires perl
      - usr/lib/*/gconv             # unicode
      - usr/lib/*/gdk-pixbuf-2.0/*/loaders/libpixbufloader-??[!g]*.so # only png & svg are required
      - usr/lib/*/glib-2.0
      - usr/lib/python*/cgi.py
      - usr/lib/python*/test
      - usr/lib/python*/unittest
      - usr/share/doc
      - usr/share/glib-2.0
      - usr/share/gtk-doc
      - usr/share/icu
      - usr/share/locale
      - usr/share/man
      - usr/share/python3/runtime.d
      - usr/share/thumbnailers

  runtime:
    preserve:
      - usr/lib/python*/site-packages/scc/input-event-codes.h
      - usr/lib/python*/dist-packages/scc/input-event-codes.h

    env:
      PYTHONPATH: "${APPDIR}/usr/lib/scc-site-packages:${APPDIR}/usr/lib/python3/dist-packages:${PYTHONPATH}"
      SCC_SHARED: "${APPDIR}/usr/share/scc"

AppImage:
  arch: "{{APPIMAGE_ARCH}}"
  update-information: "gh-releases-zsync|C0rn3j|sc-controller|latest|sc-controller-*-{{APPIMAGE_APT_DISTRO}}-{{APPIMAGE_ARCH}}.AppImage.zsync"
